import React from 'react';

import Button from '@atlaskit/button';

const ButtonPrimary = ({buttonName}) => {
  return <Button appearance="primary">{buttonName}</Button>;
};

export default ButtonPrimary;